# Molecule managed
---
file:
  /usr/bin/hugo:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "hugo --help"
    stdout:
      - "/hugo/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
