# Molecule managed
---
file:
  /usr/bin/secure:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "secure --help"
    stdout:
      - "/secure/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
