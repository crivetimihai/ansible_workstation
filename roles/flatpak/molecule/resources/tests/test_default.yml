# Molecule managed
---
file:
  /usr/bin/flatpak:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "flatpak --help"
    stdout:
      - "/flatpak/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
