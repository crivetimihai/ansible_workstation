# Molecule managed
---
file:
  /usr/bin/anaconda:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "anaconda --help"
    stdout:
      - "/anaconda/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
