# Molecule managed
---
file:
  /usr/bin/dotfiles:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "dotfiles --help"
    stdout:
      - "/dotfiles/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
