# Molecule managed
---
file:
  /usr/bin/cloudcli:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "cloudcli --help"
    stdout:
      - "/cloudcli/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
