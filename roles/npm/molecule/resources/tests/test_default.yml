# Molecule managed
---
file:
  /usr/bin/npm:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "npm --help"
    stdout:
      - "/npm/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
