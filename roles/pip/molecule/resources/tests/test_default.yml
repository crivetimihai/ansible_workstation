# Molecule managed
---
file:
  /usr/bin/pip:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "pip --help"
    stdout:
      - "/pip/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
